{
  "name": "afrimomo",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "format": "pnpm -r format",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.3"
  }
}